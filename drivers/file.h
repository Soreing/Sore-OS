#ifndef FILE_H
#define FILE_H

struct File
{   int clusters[8];
};

// Gets the long file name of a file in the current directory
//void getLongFileName(struct File folder, char* buffer, int size);


#endif